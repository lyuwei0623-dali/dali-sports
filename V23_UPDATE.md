# V23：自動快照模型／市場分離

這版修正會員端把「缺少可下注盤口」誤顯示成「整場沒有運算結果」的問題。

## 顯示規則

- 綠色：已保存完整實際盤口、價格，且依既有規則通過正 EV 檢查的推薦。
- 藍色 `模型傾向`：已保存的模型預測可用，但當下沒有完整可驗證的實際獨贏、讓分或大小盤。此狀態不會製造盤口、賠率或 EV。
- 灰色 `無法評估`：該場連模型資料也沒有保存；重新建立該日期的自動快照即可補齊新快照。

## 不變的規則

- MLB 的 SUPER 特殊盤口、足球亞洲盤、正 EV 門檻和結算規則完全不變。
- 會員端仍只讀取已保存快照；不抓 API、不重算、不寫入資料。
- 人工發布仍優先於自動快照。

## 覆蓋方式

請使用本完整包覆蓋 GitHub 專案根目錄。若只做最小更新，必須同步覆蓋：

- `integration_adapter.py`
- `core_shared_ui.py`
- `member_experience.py`

更新後，管理員重新建立一次新的 MLB 與 Football 自動快照；既有舊快照不會被偷偷改寫。
